import React from 'react'
import ReactDOM from 'react-dom/client'
import LaunchLogWindow from './LaunchLogWindow'
import '../index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LaunchLogWindow />
  </React.StrictMode>,
)
