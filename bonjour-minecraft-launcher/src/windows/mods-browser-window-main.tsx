import React from 'react'
import ReactDOM from 'react-dom/client'
import ModsBrowserWindow from './ModsBrowserWindow'
import '../index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ModsBrowserWindow />
  </React.StrictMode>,
)
